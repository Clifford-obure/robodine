import { Outlet } from "react-router-dom";

const Landing = () => {
  return (
    <div>
      {/* <h1>Landing header</h1> */}
      <Outlet />
    </div>
  );
};

export default Landing;
